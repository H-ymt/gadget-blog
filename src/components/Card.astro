---

---

<div class="card">
  <slot />
</div>

<style lang="scss">
  .card {
    padding: 30px 24px;
    border-radius: var(--radius);
    box-shadow: 0px 0px 10px -7px var(--color-shadow);
    background: #ffff;
  }
</style>
