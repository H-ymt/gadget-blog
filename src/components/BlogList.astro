---
import BlogListItem from './BlogListItem.astro'
---

<ul class="blogList">
  <BlogListItem />
</ul>

<style lang="scss">
  .blogList {
    display: grid;
    justify-items: center;
    gap: 24px 16px;

    @include mq(sm) {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
