---
import BlogListItem from './BlogListItem.astro'
---

<ul class="blogList" data-pagefind-ignore>
  <BlogListItem />
</ul>

<style lang="scss">
  .blogList {
    display: grid;
    gap: 52px 24px;
    justify-items: center;

    @include mq(sm) {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
